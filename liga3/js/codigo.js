$(function(){
    var config = {
        
        reg: {
            
            'fecha_nacimiento' : {
                
            }
        
        }
        
        };
    $('form').liga("AJAX",config);
    });
